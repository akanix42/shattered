import Signal from "./gameObjects/Signal";

const act = Signal.create(function (entityId: number): number { return 0; });
const prepMove = Signal.create(function (entityId: number): number { return 0; });
// const addComponent
